-------------------------------------
16/09/25 todo list

WEEK 1 - 
role seperation completion (Emmanuel)
promotion system (Emmanuel)
report zipping (Joshua)
email system on teacher account creation (Joshua & Albert)
implement forgot passowrd logic (Joshua)

WEEK 2 - data integration
add teacher & student data (Emmanuel)
add top class data (Emmanuel)
 - add class average to class service (for main page)

internal date of ship: 6th Oct

OTHER:

1. speed up queries
 - fix double API calls (weird) [Emmanuel] (done)
 - simple caching & memoization [Joshua]
 - prisma acceleration (https://www.prisma.io/accelerate) [Emmanuel]

additional features (later)
edit mode

-------------------------------------

-------------------------------------
THE GREAT LOCK-IN OF 2025
> login: (non MVP)
 - on login page, also check OTP.
 - if otp is valid, and correct, and for provided mail, then redirect to change password
 - make sure that even if OTP is correct but expired or mustchangepassword is false, it can't work
 - have a seperate section for OTP (for user comprehension)

password resetting:
if teacher, then 2 appproaches

approach 1:
1. send request to admin's email. he opens link, which prompts to authorize.
2. next time the teacher tries to access, he is redirected to reset password

approach 2: do the whole process but for user rather than admin.

choose whichever is easier to implement, within reasonable security.

> role seperation
1. grades
2. comments
3. reports (done)

> hosting
1. onrender
2. database + server monthly costs
3. final yearly figure

> other
1. make reports output a timestamped zip

-------------------------------------

main:
 - login:
   . fix JWT cookie bug (done)
   . ensure sessions work
   . fix the 'requirement' of sign-in on reload (use localstorage)
   . handle accounts, editing, PIN, changing passwords
 - roles + perms
   NOTE: users cannot access what the website does not fetch

 - students page (done):
   . implement the subject offerings (right now, it seems the entire catalog is being fetched) (done)
   . edit student modal momentarily disappears (done)   > "edit profile" modal
     . prefill fields with queried data (student details section)
     . edit student fields with: name, parent emails, "id (school pay code, same thing)", subject offering (see below)     . fetch all classes and the number of students (requires backend editing)
     . the subject selection for that class depends on the above
   > "add new student" modal
     . largely the same as above, except the backend endpoints are different (creating new data)
     . underlying UI is the same
     . this modal also somehow disappears momentarily; needs fixing.

- teachers page: (almost done)
- classes page: (i'm getting cooked)
 - intended functionality:
   . edit only
- grades page: (done, mostly)
- comments page: (done, mostly)
- reports page: (done, mostly)
- school page: (almost done)
  . make promotions
- settings page:

change all avatars to placeholder nothings lol
