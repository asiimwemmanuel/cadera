(all specifications below are for a teacher, not admin. ensure to maintain behaviour for admin)

if user_role is "TEACHER" in LocalStorage, then when loading classes,

limit it to classes in assignments & advisory, not just any class. ensure to show clear distinction in these 2 categories.

when accessing an assignment, limit the sidebar to only the subjects in assignment.
when accessing an advisory, limit the sidebar to 'Class Teacher'
never access the headteacher's comment

adjust comments section (visually) for proper editing (only for teachers)

note; ensure comments always have a liberal amount of space for viewing, for any user role.
